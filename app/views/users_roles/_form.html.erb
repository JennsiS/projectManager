<%= form_with(model: users_role) do |form| %>
  <% if users_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(users_role.errors.count, "error") %> prohibited this users_role from being saved:</h2>

      <ul>
        <% users_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :role_id, style: "display: block" %>
    <%= form.text_field :role_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
